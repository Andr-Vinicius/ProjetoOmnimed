import 'package:flutter/material.dart';

class LogadoView extends StatelessWidget {
  const LogadoView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
